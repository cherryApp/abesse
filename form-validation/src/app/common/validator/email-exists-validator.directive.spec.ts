import { EmailExistsValidatorDirective } from './email-exists-validator.directive';

describe('EmailExistsValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailExistsValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
